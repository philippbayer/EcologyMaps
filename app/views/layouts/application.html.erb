<!DOCTYPE html>
<html>
<head>
  <title>eUcoMap</title>
  <%= stylesheet_link_tag :all %>
  <%= csrf_meta_tag %>
<style type="text/css">
.middle {vertical-align:middle}
</style>
</head>
<body>
<div class="header round">

		<%=link_to image_tag("/images/logo.png", :style => "vertical-align:middle"),:root %><b> <%= link_to("FAQ",:controller=> "static", :action => "faq") %>
		
		
		<% if session[:map_type] == "suff" %>
		| <%= link_to("Switch to colours according to Habitat",HabiYear.find_by_year(@suff_year.year)) %>
		<% elsif session[:map_type] == "habi" %>
		| <%= link_to("Switch to colours according to Sufficiency",SuffYear.find_by_year(@habi_year.year)) %>
	<% end %>

</b>
</div>
<%= yield %>
</body>
  <%= javascript_include_tag :defaults %>
</html>
