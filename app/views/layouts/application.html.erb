<!DOCTYPE html>
<html>
<head>
  <title>eUcoMap</title>
  <%= stylesheet_link_tag :all %>
  <%= csrf_meta_tag %>
</head>
<body>
<div class="header round">
<center>
	<img src="/images/logo.png">
	<table>
	<tr>
		<td align="right"><%= link_to("Home", :root) %></td> 
		<td align="left"><%= link_to("FAQ",:controller=> "static", :action => "faq") %></td>
	</tr>
	</table>

<table>
	<tr>
		<% if session[:map_type] == "suff" %>
		<td align="right"><%= link_to("Switch to colours according to Habitat",HabiYear.find_by_year(@suff_year.year)) %></td>
		<% elsif session[:map_type] == "habi" %>
		<td align="left"><%= link_to("Switch to colours according to Sufficiency",SuffYear.find_by_year(@habi_year.year)) %></td>
	<% end %>
	</tr>
</table>
</center>
</div>
<%= yield %>
</body>
  <%= javascript_include_tag :defaults %>
</html>
