<!DOCTYPE html>
<html>
<head>
  <title>eUcoMap</title>
  <%= stylesheet_link_tag :all %>
  <%= csrf_meta_tag %>
</head>
<body>
<div class="header">
<center>
<b>eUcoMap</b>
	<table>
	<tr>
		<td align="right"><%= link_to("Home", :root) %></td> 
		<td align="left"><%= link_to("FAQ","/faq.html") %></td>
	</tr>
</table>
<table>
	<tr>
		<% if session[:map_type] == "suff" %>
		<td align="right"><%= link_to("Switch to colours according to Habitat",HabiYear.find_by_year(2010)) %></td>
		<% else %>
		<td align="left"><%= link_to("Switch to colours according to Sufficiency",SuffYear.find_by_year(2010)) %></td>
	<% end %>
	</tr>
</table>
</center>
</div>
<%= yield %>
</body>
  <%= javascript_include_tag :defaults %>
</html>
